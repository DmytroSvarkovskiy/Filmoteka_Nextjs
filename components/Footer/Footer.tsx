import { Container } from '@/components';
import styles from './styled.module.scss';
export const Footer = () => {
  return (
    <section className={styles.section}>
      <Container>gtrgt</Container>
    </section>
  );
};
